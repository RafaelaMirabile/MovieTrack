export type Movie = {
	id?: number,
	title: string,
	banner: string,
	genre: string,
	rate?: number,
	review?: string,
	create_at?: Date
};

export type Review = {
	review: string
};

export type Rate = {
	rate: number
};





